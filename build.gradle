plugins {
  id "org.sonarqube" version "4.2.1.3168"
}

sonar {
  properties {
    property "sonar.projectKey", "kodmain_kitsune"
    property "sonar.organization", "kodmain"
    property "sonar.host", "https://sonarcloud.io"
  }
}